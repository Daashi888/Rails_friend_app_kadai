<h1>投稿内容一覧</h1>

<p><%= notice %></p>

<table>
  <tr>
    <th>画像</th>
    <th>内容</th>
  </tr>

<% @pictures.each do |picture| %>
  <tr>
    <td><%= image_tag picture.image.url, size:"160x160" %></td>
    <td><%= picture.content %></td>

    <td><%= link_to '詳細を確認する', picture_path(picture.id) %>
    <%= link_to "投稿内容を編集する", edit_picture_path(picture.id), data: { confirm: '本当に編集していいですか？'} %>
    <%= link_to '投稿を削除する', picture_path(picture.id), method: :delete, data: { confirm: '本当に削除していいですか？'} %>
    ユーザーネーム:<%= picture.user.name %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to '新しく記事を投稿する', new_picture_path, class:"btn btn-success" %>